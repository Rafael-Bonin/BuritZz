const express = require('express');

const listRouter = express.Router();

listRouter.get('/', );
listRouter.post('/', );
listRouter.put('/', );
listRouter.delete('/', );

module.exports = listRouter;